{
  "name": "blogapp",
  "version": "1.0.0",
  "description": "A full-stack blog application with AI-powered content autocomplete, built with Express, React (Vite), and MongoDB.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix BlogAppFrontend",
    "dev": "npm run dev:backend & npm run dev:frontend",
    "build:backend": "npm run build --prefix Backend",
    "build:frontend": "npm run build --prefix BlogAppFrontend",
    "build": "npm run build:backend && npm run build:frontend",
    "install:all": "npm install && npm install --prefix Backend && npm install --prefix BlogAppFrontend"
  },
  "author": "",
  "license": "ISC"
}
